export type ColorType = {
  primary: string;
  secondary: string;
  success: string;
  danger: string;
  warning: string;
  live: string;
  playerBackground: string;
};


export type UserTheme = {
  colors: ColorType;
};
